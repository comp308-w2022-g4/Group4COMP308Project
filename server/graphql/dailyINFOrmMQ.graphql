input NewDailyInfo {
  id: ID!
  pulseRate: String
  bloodPressure: String
  weight: String
  temperature: String
  respiratoryRate: String
  patient: ID!
}

type Query {
  getDailyInfo(id: ID!): dailyINFO!
}

type Mutation {
  dailyINFO(generalInfo: NewDailyInfo): dailyINFO
}